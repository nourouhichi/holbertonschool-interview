#ifndef ADVANCED_SEARCH
#define ADVANCED_SEARCH
#include <stdio.h>
#include <stdlib.h>
int advanced_binary(int *array, size_t size, int value);
void print_array(int *array, size_t i, size_t size);
#endif
